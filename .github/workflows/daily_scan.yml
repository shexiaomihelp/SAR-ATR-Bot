name: Daily Trading Bot

on:
  schedule:
    - cron: '0 0 * * *' # 每天台灣時間早上 8:00 (UTC 00:00) 執行
  workflow_dispatch: 

jobs:
  run_bot:
    runs-on: ubuntu-latest
    
    env:
      LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
      LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
    
    steps:
      - name: 📥 下載程式碼
        uses: actions/checkout@v4

      - name: 🐍 設定 Python 環境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 安裝必要套件
        # 確保此行縮排正確
        run: pip install yfinance pandas pandas_ta requests lxml html5lib

      - name: 🚀 執行選股掃描
        run: python trading_scanner.py 1
