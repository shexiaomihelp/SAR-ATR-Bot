- name: 📦 安裝必要套件 (強制執行)
        # 這裡使用 | 符號將多行命令視為一個步驟，確保安裝成功
        run: |
          pip install yfinance
          pip install pandas
          pip install pandas_ta
          pip install requests lxml html5lib
          
      - name: 🚀 執行選股掃描
        run: python trading_scanner.py 1
